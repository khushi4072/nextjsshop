import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  images: {
    domains: ['images.unsplash.com', 'example.com'], // Add the Unsplash domain and your image hosting site domain
  },
  /* config options here */
};

export default nextConfig;
